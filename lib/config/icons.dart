import 'package:flutter/material.dart';

Widget iconNavPostList(Color color) => Icon(
      Icons.art_track_rounded,
      color: color,
    );

Widget iconNavPost(Color color) => Icon(
      Icons.chrome_reader_mode_outlined,
      color: color,
    );
