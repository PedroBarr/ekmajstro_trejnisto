import 'package:flutter/material.dart';

Widget iconNavPostList(
  Color color,
) =>
    Icon(
      Icons.art_track_rounded,
      color: color,
    );

Widget iconNavPost(
  Color color,
) =>
    Icon(
      Icons.chrome_reader_mode_outlined,
      color: color,
    );

Widget iconNavSection(
  Color color,
) =>
    Icon(
      Icons.pages_outlined,
      color: color,
    );

Widget iconNavTagList(
  Color color,
) =>
    Icon(
      Icons.label_important,
      color: color,
    );

Widget iconNavResourceList(
  Color color,
) =>
    Icon(
      Icons.file_present_sharp,
      color: color,
    );

IconData iconSectionMarkOne = Icons.stars;
